

package client.controllers;

import client.views.*;

public class Client {
    /*
    This handles waiting around for the server to respond, maintains a client View to display, along with
     */
    public Client(int port, ClientView view){
        // this should instantiate a view
    }
}
